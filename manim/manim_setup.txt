sudo apt install ffmpeg
#export PYTHONPATH="/home/python/manim:$PYTHONPATH"
sudo apt install texlive texlive-latex-extra texlive-fonts-recommended dvipng
snap list
    sudo snap remove code
    wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
    sudo install -o root -g root -m 644 packages.microsoft.gpg /usr/share/keyrings/
    sudo sh -c 'echo "deb [arch=amd64 signed-by=/usr/share/keyrings/packages.microsoft.gpg] https://packages.microsoft.com/repos/vscode stable main" > /etc/apt/sources.list.d/vscode.list'
    sudo apt update
    sudo apt install code
sudo apt install libcanberra-gtk-module libcanberra-gtk3-module
sudo locale-gen
sudo dpkg-reconfigure locales
xdg-open /home/python/media/images/sin_plot/SinAndCosFunctionPlot_ManimCE_v0.18.1.png
sudo locale-gen en_US.UTF-8
sudo update-locale
sudo apt install libcanberra-gtk-module
sudo apt install --reinstall eog
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
CANBERRA_MODULE_DEBUG=1 eog
sudo apt install --reinstall libgtk-3-0
